# TCCC Evaluation Suite v3.0.1

## Scope
Surgical fix for the class-level **More Exports** control only.

## Fix
- Replaced the fragile native `<details>` dropdown with an explicit button and controlled menu panel.
- Restored visible/selectable access to every existing secondary class export:
  - Summary CSV
  - Criteria CSV
  - Timer CSV
  - Event / Audit CSV
  - Performance Analytics CSV
  - Enterprise Detail CSV
  - Backup JSON
- Export implementations, data, filenames, scoring, timers, curriculum, analytics, RCA/performance contributors, local storage, and all other workflows are unchanged.
